node_modules/
build/
dist/
dist-worker/
.wrangler/
*.tsbuildinfo
.DS_Store

# Coverage output (c8)
coverage/
.nyc_output/

# Local secrets — never commit. Your SF_API_KEY lives here.
.env
.env.*
!.env.*.example
*.mcpb
.vercel

# Internal working docs (specs, handoffs, runbooks, launch/distribution material).
# This repo is PUBLIC — anything here is untracked so planning notes, pricing
# thinking, and operator runbooks stay local. Nothing in the shipped package or CI
# reads from here; user-facing docs belong in README.md / CONTRIBUTING.md instead.
.local-docs/
